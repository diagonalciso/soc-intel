# Web framework
fastapi==0.139.0
uvicorn[standard]==0.50.0
python-multipart==0.0.32

# GraphQL
strawberry-graphql[fastapi]==0.320.1

# Database - PostgreSQL
sqlalchemy==2.0.51
alembic==1.18.5
asyncpg==0.31.0
psycopg2-binary==2.9.12

# OpenSearch
opensearch-py==3.2.0

# Redis
redis==8.0.1
hiredis==3.4.0

# RabbitMQ
aio-pika==9.6.2

# MinIO
minio==7.2.20

# Auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
email-validator==2.3.0  # for pydantic EmailStr (was a core dep of fastapi<0.112, now an extra)
# pyasn1 stays 0.4.8: python-jose 3.4.0 hard-pins pyasn1<0.5.0, so the fix for
# CVE-2026-30922 (0.6.3) is unreachable without replacing python-jose. Accepted
# risk — pyasn1 is only reached via rsa for parsing trusted keys.

# STIX
stix2==3.0.2
taxii2-client==2.3.0

# HTTP client
httpx[socks]==0.28.1
aiohttp==3.14.1

# Pydantic
pydantic==2.13.4
pydantic-settings==2.14.2

# Utils
python-dotenv==1.2.2
structlog==26.1.0
tenacity==9.1.4
schedule==1.2.2
APScheduler==3.11.3

# Dark web / network
PySocks==1.7.1
stem==1.8.2
beautifulsoup4==4.15.0
lxml==6.1.1
telethon==1.44.0

# Data processing
pandas==3.0.3

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
httpx[socks]==0.28.1
